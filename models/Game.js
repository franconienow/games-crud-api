export default class Game {
  constructor(id, name, publisher, release, genres) {
    this.id = id;
    this.name = name;
    this.publisher = publisher;
    this.release = release;
    this.genres = genres;
  }
}
